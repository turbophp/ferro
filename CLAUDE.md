# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> This repo is the **Ferro implementation charter**. `ferro-spec-v0.2.md` is the contract; this file is the working agreement. Read the spec in full before writing any code.

## Current state (read first)

**M0 is complete and merged** (SPEC §22.1): the full engine vertical — `/proto` registry, wire codec, `ferrod` session layer, the hand-rolled PG pool, SQL EXEC + TX services, the PHP sync client, and the D12 bench — works end-to-end against real Postgres, with the D12 p99 measurement recorded in `bench/results/`.

**M1 is in progress.** M1-S1 (the real PG protocol-signal pin engine — `Client::transaction_status()` off a vendored `tokio-postgres` fork, replacing the M0 TX-lifecycle stub) is complete: the pool reads the RFQ `I`/`T`/`E` byte after every statement and pins/taints from it, with an unconditional Err-arm fail-safe for the cases where that byte can't be trusted. **M1-S2** (the assist lexer, `ferro-classify`) is complete: a dialect-aware keyword classifier that detects protocol-invisible session mutations — `LISTEN`/`UNLISTEN`, non-xact advisory locks, raw `PREPARE`/`EXECUTE`/`DEALLOCATE`, temp-object DDL, non-local `SET` — with a `pin_on_unknown` default and a per-pool `pin_functions` escape hatch, wired into `Checkout::apply_classify` as the ASSIST signal that taints the connection for hygiene; RFQ (S1) remains the transaction AUTHORITY (assist-not-authority). **M1-S3 (conditional hygiene at checkout) is complete** — a per-state reset profile driven by the S1/S2 pin state: a **tainted** connection gets the full `DISCARD ALL`; a **non-tainted** recycled PG connection gets a **targeted** profile (`CLOSE ALL; SET SESSION AUTHORIZATION DEFAULT; RESET ALL; UNLISTEN *; SELECT pg_advisory_unlock_all(); DISCARD TEMP; DISCARD SEQUENCES;`) that closes every session-state leak class — including the §7.4 assist-lexer blind spots (a `set_config(...,false)` GUC, an in-`DO`-body advisory lock, a `WITH HOLD` cursor) — while preserving the engine's future namespaced prepares. The §7.2 "pipelined ahead of the first user statement" optimization is **DEFERRED** to a later perf slice (hygiene runs awaited-at-checkout for now). See `docs/superpowers/specs/2026-07-28-ferro-m1-execution-design.md` for the full M1 slice plan and rationale. **M1-S4 (the full §9.2 error taxonomy + write-fate matrix) is complete** — a central `fate.rs` `classify_fate(err, OpContext)` matrix is the ONE place a `PoolError` becomes a wire `ErrorPayload`, folding in the §19.3 `Indeterminate` rules (readonly/sent/in_tx-gated) and a `57014` (cancel/`statement_timeout`) override; `timeout_ms` + a per-request `CANCEL` are now ENFORCED on both the autocommit and tx-scoped EXEC paths — a cancelled/timed-out autocommit write drains to `Indeterminate` (unconfirmed non-execution), while an in-tx one rolls the transaction back and tombstones the `tx_id` → `Retryable` (the tx's fate is known: it will never commit); a serialization failure or deadlock — including one caught at COMMIT, the dominant real-world SERIALIZABLE-conflict shape (SSI defers the pivot check to COMMIT) — classifies `Retryable`. A live chaos suite (`ferrod`'s `chaos_fate_it.rs`) proves every §19.3 branch against real Dockerized Postgres and that a write is applied at-most-once (no transparent retry, ever — charter rule 3). **M1-S5 (the windowed streaming DATA-channel producer — the M0 D-S5-1 deferral) is complete** — a `fetch:stream` EXEC now emits a `HEAD(cols)` frame + N `DATA(rows)` frames + exactly one terminal `END`, streamed incrementally (constant engine memory) under a per-request **credit window** (default 64 frames / 16 MiB, replenished by `WINDOW_UPDATE`) bounded by a per-session **cap** (16 MiB), for BOTH autocommit and tx-scoped requests. The `/proto` `service::STREAM` `HEAD`/`DATA` methods + hand-rolled `StreamHead`/`StreamData` codec + golden vectors landed in both codecs (charter rule 2); the backend pulls rows incrementally via `Checkout::query_stream` → `RowStreamHandle` (mandatory `finish` + a `Drop` force-taint net closes the cross-tenant-leak class, charter rule 6); the session gained a cancel/deadline-aware `CreditCell` (B3 — a producer parked on backpressure never hangs: it resumes on `WINDOW_UPDATE` or unwinds on cancel/timeout, always reaching exactly one END) and a `SessionCap` whose `CapReserve` RAII guard releases exactly-once on the writer's post-write drop (M6); DATA + the terminal share ONE ordered `control_tx` conduit (via a `ControlMsg { frame, cap }` wrapper) so the terminal never overtakes a DATA frame (B4, by construction — the 2-channel control/data priority-split is **DEFERRED** to a perf slice, charter rule 5); and the S4 §19.3 fate matrix (`fate.rs`) is reused verbatim — a cancelled/timed-out streamed **write** is `Indeterminate` (autocommit) or `Retryable`+rollback+tombstone (in-tx), a streamed read **whose client DECLARED it `readonly`** is never `Indeterminate` (the engine never infers read-vs-write; §22.2 (ac) records what that costs the M1-S8b DBAL tier, which declares every statement a write), and the stream OPEN itself is timeout/cancel-raced. `default_credit_bytes` was coupled to `MAX_FRAME_PAYLOAD` (16 MiB) so any single valid frame always fits the initial window (the large-row rule; user-chosen Option B, superseding the earlier "do NOT couple" note — SPEC §5.2/§22.2). The PHP client gained a dependency-free lazy `stream()` Generator (never buffers; sends `WINDOW_UPDATE` as it consumes; on abandonment sends an outbound `CANCEL` + drains so the wire never desyncs). A live e2e (`ferrod`'s `stream_it.rs`) proves a 50 000-row result streams in order under a 2-frame window with exactly one END, backpressure genuinely engaged (a stall probe), the session cap returning to baseline, and abandonment truncating (`CANCEL` at 3072/50 000) + recovering — against real Dockerized Postgres. The plan was adversarially verified before execution (6 blockers + 5 majors caught pre-build). **M1-S6 (the MySQL/MariaDB backend + the S2 assist-lexer's tracker verification against a second dialect) is complete** — a second engine now fills the `PoolBackend` seam behind the same chassis, proving the M0 abstraction is service-agnostic, with the pin AUTHORITY **split** by construction: transaction-open is read fork-free from `OkPacket::status_flags() & SERVER_STATUS_IN_TRANS` (uniform across MySQL 8 + MariaDB 11), while session-mutation taint comes from OK-packet **session-state trackers** (`SessionStateChange`), which required a **vendored `mysql_async` fork** that ORs `CLIENT_SESSION_TRACK` into the negotiated capabilities (mirroring the S1 `tokio-postgres` fork pattern — `[patch.crates-io]` redirect + drafted-not-filed `UPSTREAM_PR_MYSQL_ASYNC.md`); MySQL has no aborted-tx state, so `TxStatus::Failed` is NEVER produced from a MySQL signal (only via `error_map`). The tracker signal is wired into `ferro-pool` additively — `PoolBackend::take_session_mutated` (default-false, a PG no-op) consumed by `Checkout::apply_session_tracker` after `apply_tx_status` on the instrumented methods, guarded so it never clobbers the RFQ/`PinCause::Tx` authority (new `PinCause::SessionTracker`). The pool registry became an `AnyPool { Pg | Mysql }` enum (kind inferred from the DSN scheme) with the exec/begin bodies generic-extracted (`run_exec_on_pool<B>` / `begin_on_pool<B>`) so the PG path stays behaviorally identical (90 live PG tests green, zero regression); `fetch:stream` on a MySQL pool returns a clean `Unsupported` (streaming deferred to **S7**, §22.2). The assist lexer gained a `classify_one_mysql` dialect path (`GET_LOCK`/`RELEASE_LOCK`, `CREATE TEMPORARY`, raw `PREPARE`, `LOCK TABLES`, `SET SESSION`) plus an **unconditional `CALL`/`DO` pin** (the in-stored-proc-mutation backstop, firing even under `pin_on_unknown=false`), leaving the shared PG classifier untouched. Hygiene recycles every reused MySQL conn via `COM_RESET_CONNECTION` (`clean_reset_profile()=Some(Full)`; the tracker-clean None-skip is a §16 optimization DEFERRED, R2). The S4 `fate.rs` §19.3 matrix is **reused verbatim** (untouched): `error_map` keys errno → fate (1213 deadlock / 1205 lock-wait-timeout → `Retryable`; 1317 / 3024 MySQL / 1969 MariaDB timeout → `CANCELLED`→`57014`; 1062 dup-key → `Unique`/non-retryable; transport → `ConnectionLost`), and a live chaos suite (`ferrod`'s `mysql_chaos_it.rs`) proves every §19.3 cell against BOTH real Dockerized engines with an `information_schema.processlist` in-flight guard (no false-green) and a read-back at-most-once proof: timeout/`CANCEL`/conn-`KILL` autocommit write → `Indeterminate`, in-tx cancel → `Retryable`+rollback+tombstone, deadlock 1213 → `Retryable` (survivor applied exactly once), and the 1205-leaves-tx-`InTx` case tears down to exactly one `Retryable` terminal (no stuck tx). A self-contained hard gate (`pin_hygiene_it.rs`) proves the tracker detects an in-`CALL` `SET SESSION` on a raw backend conn (unmaskable by the CALL lexer) and that `COM_RESET_CONNECTION` closes the read-back leak, on both engines. The §9.1 type policy was pinned (`BIGINT UNSIGNED`/`DECIMAL`/`DATETIME` → loud `Unsupported`, `TINYINT(1)` → `Bool`) so S8's DBAL suite inherits a documented contract. The plan was adversarially verified pre-build (the `mysql_async`-as-is assumption — that it negotiates `CLIENT_SESSION_TRACK` — was proven FALSE before any code; the fork is required); the whole-branch review's adversarial pass caught one §12 secret-hygiene leak (`infer_pool_kind` logging a schemeless credential-bearing DSN), fixed + regression-tested. **M1-S7 (canonical TYPE COVERAGE — a slice inserted ahead of the driver tier, renumbering the old S7→S8→S9→S10; §22.2) is complete** — all **14** registry tags (`NULL`, `BOOL`, `I64`, `U64`, `F64`, `DECIMAL`, `TEXT`, `BYTES`, `DATE`, `TIME`, `TIMESTAMP`, `TIMESTAMPTZ`, `UUID`, `JSON`) are now READ **and** BOUND end to end across **PG 17, MySQL 8.4 and MariaDB 11.8**, on the M0 scalar boundary that made the original DBAL-suite exit gate unreachable. The wire payload is **text-canonical**: every tag *added in S7* rides the msgpack `str` family carrying the lossless, policy-independent canonical text of `proto/PROTOCOL.md` §3.2 (`U64` alone rides the uint family; the M0 tags keep their native families, and it is exactly that surviving `str`/`bin` split — indistinguishable in PHP after unpack — that forced text over a binary form), so a `DECIMAL`'s display scale, a 131 072-digit numeric, `NaN`/`±Infinity`, PG's legal `24:00:00` and a MySQL `TIME`'s ±838 h all survive; the backend renders, the codecs move bytes and validate nothing past UTF-8, and a golden vector per tag landed in both codecs (charter rule 2). The **implemented-tag set is hashed** — `/proto/types.toml` gained a real, parsed `implemented` list feeding `registry.lock.json` → `TYPE_REGISTRY_HASH` — so an engine/client pair with different type coverage fails fast **at the handshake** instead of throwing mid-query on the first row of a newly-supported type; the live acceptance gate (`ferrod`'s `types_e2e_it.rs`) DERIVES its per-tag produce/bind assertions from that same list, so a coverage claim cannot outrun the code. **MySQL/MariaDB sessions are pinned to `time_zone = '+00:00'`** (re-applied after every `COM_RESET_CONNECTION`), forced by §9 mapping MySQL `timestamp` → `TIMESTAMPTZ` while the driver hands back zone-*less* components, and required for pooling determinism — **visible drop-in consequence: `NOW()`/`CURDATE()`/`CURTIME()` return UTC on every Ferro MySQL connection**, the same choice Doctrine and Laravel make. The four §9.1 policies (`decimal`, `uuid`, `u64_overflow`, `naive_datetime_zone`) shipped **CLIENT-SIDE** as a `TypePolicyOptions` value object (the planned `PoolConfig`/`ferrod` knobs were *dropped, not deferred* — the wire is policy-independent, so an operator setting one would observe nothing), decoding into the §9 value objects (`Ferro\{Decimal, Date, Time, Uuid, Json, U64, NaiveTimestamp}`) with the naive/instant bind rule that makes a read → write-back round trip byte-stable; a policy refusal is a `TypePolicyException` raised client-side (pre-send in `bindOne` or post-success in `decodeRow`), deliberately **outside** the §9.2 fate branches, while a malformed payload stays `ProtocolException`. Non-representable values are loud, never coerced: dates outside `0001-01-01 .. 9999-12-31` are an `Unsupported` naming the year, PG refuses a `U64` bind for every type (no unsigned integer type exists there), and MySQL refuses a payload with no MySQL representation **pre-send** — because under a permissive `sql_mode` (which Doctrine never sets) the server silently COERCES `infinity`/`NaN`/`839:00:00` instead of erroring, which measured live as a silent corrupt write. Documented-by-design asymmetries: no MySQL-family backend ever emits `UUID` (MariaDB's native `UUID`/`INET4`/`INET6` reach the wire as `MYSQL_TYPE_STRING`), MariaDB `JSON` is a `LONGTEXT` alias → `TEXT`, and `TAG_BYTES` is currently **unreachable from PHP** (every PHP string binds `TEXT`). One shipped defect was recorded rather than fixed at S7: PG's full hygiene `DISCARD ALL` includes `DEALLOCATE ALL`, which permanently poisons `tokio-postgres`'s typeinfo statement cache, so a *second, distinct* CUSTOM OID on a reset connection degrades from the loud `Unsupported` to a bare `26000` (safety intact — `NonRetryable`, never `Indeterminate`) — SPEC §22.2 (m) + `docs/followups/2026-08-06-discard-all-typeinfo-cache-poisoning.md`. **It is FIXED as of the S8a review round** (see S8a below); its "no shipped type is affected, all 14 map to builtin OIDs" caveat stopped being true the moment S8a made a DOMAIN-typed parameter bindable. **M1-S8a (engine/client DBAL-readiness — the foundations half of the S8 split; §22.2) is complete** — the seven carries S8b needs, built and adversarially reviewed. `fetch:stream` gained ONE streaming-capability authority (a tx-scoped MySQL stream no longer taints the pinned conn); `last_insert_id` rides the OK packet to the client; the MySQL vendor **errno** now travels the wire beside SQLSTATE (DBAL keys on the pair) — with the §19.3 `57014` override the one documented place it is dropped; the PG bind gained **narrowing** (`I64`→`int2`/`int4`/`int8`, `F64`→`float4`/`float8`) and **DOMAIN resolution on BOTH sides of the bind** via Ferro-owned newtypes, which is the `Kind::Domain` unwrap DBAL emits on every `serial` PK insert; savepoint SQL passes through inside a transaction; `BEGIN` became isolation-dialect-aware as a **single batched statement** (`SET TRANSACTION` + `BEGIN`, never `SET SESSION` — that would be a cross-tenant leak); the PHP client gained imperative `begin`/`commit`/`rollBack` and `Ferro\Bytes`, making `TAG_BYTES` reachable at last; and `HELLO_ACK` now advertises per-pool metadata (`name`, `kind`, `server_version`) at **`protocol_version` 2**, fed by a lazy, concurrent, TTL'd per-pool version probe — deliberately not a `OnceCell`, which seals on success and would advertise a stale version (and therefore a wrong DBAL platform) across a rolling backend upgrade. The whole-branch review is the reason to trust this slice: five adversarial dimensions produced **24 confirmed findings**, every one reproduced live before it counted and refuted before it was fixed. The two that mattered most: S8a's own DOMAIN bind had **re-armed the §22.2 (m) typeinfo poisoning on ordinary supported writes** — `information_schema`'s columns ARE domains (four distinct custom OIDs), i.e. exactly what S8b's schema manager touches on every introspection — now **FIXED** by a third `tokio-postgres` fork accessor (`clear_typeinfo_statement_cache()`) called on BOTH reset profiles, `Targeted` included, because `ferro-classify` safe-lists a USER-issued `DISCARD ALL` so such a connection recycles NON-tainted carrying the same dead handles; and Task 12's self-reported "detached-vs-dropped futures has no black-box observable" was **wrong** — the guard was built, and under the inlined-future mutation the next tenant demonstrably receives a connection whose server session is still running the previous tenant's statement. Also fixed: every `float8` in BOTH backends could have been silently truncated to `f32` with the whole suite green (every F64 fixture in the tree was exactly f32-representable); `in_flight` leaked on a probe panic or hang, sealing a pool permanently un-probeable; the isolation enum was hand-duplicated across Rust and PHP with no cross-language lock (a silent SERIALIZABLE→READ COMMITTED downgrade armed for the very next slice); and §22.2 (u) and (v) flatly contradicted each other. Process notes worth carrying: **all spec edits were serialized through one author**, because parallel spec authorship is what produced the (u)/(v) contradiction in the first place; and reviewers **journal each finding to disk as they find it**, after two full review rounds were killed by session limits and lost everything held in context — the first returned a clean-looking zero, indistinguishable from a genuine pass. Known residuals, recorded not buried: a user statement that deallocates WITHIN one checkout still poisons that checkout (closing it needs a per-statement hook, and under `ferrod` the window is only reachable inside one explicit transaction that first nukes its own prepares); and `probe_and_record` has no outer drop-based timeout **by choice** — such a timeout would drop a live `Checkout` mid-statement and reintroduce the very hazard the same commit closes. Gates at the boundary: **762 Rust tests live** against PG 17 + MySQL 8.4 + MariaDB 11.8, 557 PHP tests / 1890 assertions, PHPStan L9 clean, `/proto` regeneration zero-diff, no-skip gate green. **M1-S8b (the `ferro/doctrine-dbal-driver` package itself, plus the acceptance gate) is complete.** A Doctrine DBAL **4** driver whose EXECUTION layer talks to `ferrod` through `ferro/client`, with Grammar/Processor, the DBAL platforms and the stock schema managers untouched (charter rule 6) — config-only adoption via `driverClass` + `driverOptions`. **No `/proto` change**: everything the driver needed was already on the wire from S8a. Four additive `ferro/client` methods made it possible (`fetchRaw`, `streamRaw`, `poolInfo` on `SessionInterface`, an isolation parameter on `begin`) plus ONE engine bind change. The load-bearing decisions, each with its cost stated rather than softened: **every statement is fate-declared a WRITE** — the DBAL 4 SPI carries no read/write signal (`executeQuery('INSERT … RETURNING id')` is indistinguishable from a SELECT) and charter rule 6 forbids guessing from SQL text, so `readonly` is an explicit `driverOptions` opt-in; that is conservative and never costs safety, but `readonly` is read TWICE in `fate.rs` and the second is the 57014 override, so **a plain SELECT killed by a server-side `statement_timeout` surfaces as `IndeterminateWriteException`** (and with a NULL SQLSTATE, because the override rebuilds the payload) — cry-wolf on the one branch that must never be routinely retried, pinned by a live guard asserting BOTH cells, documented, and explicitly NOT to be worked around with a blanket retry (§22.2 (ac)). **PG's canonical-TEXT bind was widened** to PostgreSQL's own text-input types (`numeric`, `date`, `time`, `timestamp`, `timestamptz`, `uuid`, `json`, `jsonb`) with `accepts` and the impl moving in ONE edit and `encode_format` branching with `to_sql` — without it every dated or decimal INSERT failed on PG, since stock Doctrine stringifies them all (§22.2 (aa)). **Doctrine's stock type layer is a silently-corrupting calendar parser** (measured, no exception: `'2026-00-05'` → 2025-12-05, `'0000-00-00 00:00:00'` → -0001-11-30, PG's legal `'24:00:00'` → 00:00:00), so the driver's own `ValuePolicy` re-renders a whole-second `TIMESTAMPTZ` per platform and REFUSES what it cannot represent rather than converting it — *we refuse what PDO corrupts* (§22.2 (ab)). **`setTransactionIsolation()` is captured typed in an optional `wrapperClass`** and the raw `SET SESSION …` is refused at all THREE entry points (`exec`, `runPrepared`, `query` — the plan said two, and the hole was measured: PG's `query()` streams while MySQL's delegates, so a MySQL-only test would have called two correct) (§22.2 (ad)). **`iterate*()` streams on the PARAMETERLESS PG read path and buffers everywhere else** — the prepared path cannot stream because a stream terminal carries no `affected`, and MySQL streaming stays deferred (D-S8b-2); abandonment cancels for the canonical `foreach (iterate…) { break; }` but NOT for a bound iterator, a PHP refcount fact both shapes are tested against. **A nil `server_version` defers, then resolves with one `SELECT version()`, then FAILS LOUDLY** naming the pool — never a default platform, because a wrong platform is a silently wrong SQL dialect (D-S8b-1); the MySQL-family version string is passed through UNNORMALISED, since MariaDB is detected only by that substring. **ORM on PG is an honest, documented incompatibility, not an engineered workaround** (D-S8b-5): `IdentityGenerator` is `(int) $conn->lastInsertId()`, PG reports no key by design, so PG apps configure the SEQUENCE strategy — **drop-in is config-only for DBAL and explicitly NOT config-only for ORM on PostgreSQL**.

**The acceptance gate is the reason to trust the numbers, and the numbers are modest.** `testkit/dbal-suite.sh` clones a PINNED `doctrine/dbal 4.4.4`, replaces its `TestUtil` (upstream's honours only `driver`, never `driverClass`, and silently falls back to in-memory SQLite — **reproduced deliberately: with the contact assertion disarmed the subset reported `OK (105 tests, 211 assertions)` against SQLite 3.45.1 with zero Ferro contact, nothing skipped**), launches ONE `ferrod`, and refuses to run a single test until it has asserted `getNativeConnection() instanceof Ferro\Client\Connection` and round-tripped a real `SELECT 1`. It carries **no `docker compose down` trap of any kind**. Every family gets its own `doctrine_tests` database, never the shared `ferro` one, and a container-side reset runs before phpunit — the reset enumerates schemas from `pg_namespace` rather than a hand-written list, because the hand-written list measurably rotted (two schemas the plan did not name drifted the number 71 → 72). **Two-run reproducibility was verified on all three backends** (result line AND ordered failure set diffed, six comparisons, no differences), and the cost of skipping the reset is measured: 71 errors with it, 85 without. RECORDED: PostgreSQL 17.10 **296 passed / 374 executed**, MySQL 8.4.11 **372 / 385**, MariaDB 11.8.8 **371 / 384**. §14's stated bar was *"green on PG + MySQL + SQLite; ORM suite on PG + MySQL"*; **SQLite is impossible (no backend), the ORM suite is not run at all, and the PostgreSQL column is not near the bar** — say so plainly rather than restating the bar without SQLite. Exactly ONE category-(a) driver defect was found and fixed (we had invented a `NoIdentityValue` where the SPI defines one). The other three findings are ENGINE/CLIENT gaps this slice measured and deliberately did NOT close, each with a follow-up: **50 of PG's 78 non-passing tests share ONE cause** — DBAL's stock schema manager selects `pg_index.indkey`, an `int2vector` the PG read path has no mapping for, so **`doctrine/migrations` and schema diffing do not work on PostgreSQL today**; **16 more** are the PG bind matrix being narrower than libpq in the direction S8b did not widen (`I64` into a `text` or `bool` slot, both reached by ordinary stock-Doctrine shapes); and **an `I64` at or above 2^32 is unreadable by `php/client` on every backend and every value policy** — a pre-existing HIGH-severity defect first journalled at Task 7, re-measured here independently through the native client (`4294967295` → int, `4294967296` → throws), which makes any `bigint` PK past 4.29e9 and every epoch-millis column unreadable. Gates at the boundary: **764 Rust tests live** against PG 17 + MySQL 8.4 + MariaDB 11.8 (0 failed, no-skip gate green), `php/client` 589 tests / 1750 assertions offline + 47 live / 726 assertions, `php/doctrine-dbal` 189 / 419 offline + 35 live / 267 assertions under `--fail-on-skipped`, PHPStan L9 clean on both packages, `/proto` regeneration zero-diff.

**M1-S9 (the M1 exit gate) is COMPLETE and RE-MEASURED.** The three items standing between S8b's recorded numbers and the bar all shipped — (1) `int2vector`/`oidvector` admitted as TEXT on the PG read path (§22.2 (ae), `::text`-oracle-proven, bind direction untouched, array class still deferred); (2) the `I64 → text`/`bool` PG bind widening (§22.2 (af), `bool` value-gated to 0/1, lockstep proof re-derived); (3) the `I64 ≥ 2^32` client read defect (m1-s8c — the whole int64 range reads, with the ext-vs-pure packer conformance test) — plus B1a (the stream terminal's `affected` captured client-side; the old "no `affected` on the wire" premise was measured FALSE, §22.2 (ag) corrects (ac)) and B1b (the driver's prepared path streams on PG, `rowCount()` drain-then-answer, §22.2 (ah) — **§14's never-buffer clause is closed on PostgreSQL**). The A5 re-measurement is RECORDED (`docs/dbal-suite/2026-09-09-a5-results.md`, the on-demand `dbal-suite` workflow on GH runners, two runs per family, contact assertion + reset lines verified): **PostgreSQL 364/374 (was 296 — the predicted +68: 50 int2vector + 16 bind + 2 BigInt, so the stock PG schema manager, `doctrine/migrations` and schema diffing now work through Ferro), MySQL 374/385, MariaDB 373/384**, ordered failure sets identical across runs, and **every remaining non-pass on all three backends is in the S8b (b)/(c) triage** — deliberate §19.3 semantics or structurally-unsatisfiable tests, no driver defect, no engine gap. The §14 bar as written still lacks SQLite (no backend) and the ORM suite — C-phase debts, stated plainly.

Next up: **M1 loose ends (the dev-loop ledger's Phase B — `docs/dev-loop/LEDGER.md`)**: ~~MySQL/MariaDB `query_stream`~~ **DONE — §22.2 (n)'s engine deferral is CLOSED** (the vendored fork learned to hand an owned connection back, the pool grew a bounded `reclaim_stream` seam, and `MysqlConn` became parkable with a parked-reads-dead contract); and the DBAL driver's buffer-on-MySQL fallback (D-S8b-2) is **retired too** — both pool-kind gates are gone, so `query()`/`runPrepared()`/`iterate*()` stream on every family. ~~a `/proto` `TxNotFound` code~~ **DONE — `0x300B` (NonRetryable) now carries "that `tx_id` is not live here"** so `Protocol` is back to meaning only a wire fault; `ERR_PROTOCOL` left the client's `rollBack()` swallow list, and a malformed `TxControl` body — a client codec defect — is loud again on the one path where it used to vanish (SPEC §22.2 (ai), which marks (t)'s known cost paid off). ~~savepoint verbs in the assist-lexer safe-list~~ **CLOSED as WONTFIX — the item was stale**: the classifier does return `Unknown` for savepoint verbs, but `Checkout::apply_classify_for` skips the lexer entirely on a `SavepointPassthrough` verdict, so it is unreachable (measured: `tainted=false` through a real `Checkout`), and M1-S8a had already considered widening `SAFE_LEADING_KEYWORDS` and rejected it in writing as the WIDER, less safe option. ~~the unbounded backend dial~~ **DONE — the dial is bounded** by `checkout_timeout` in `Pool::checkout`, and both backends' out-of-band cancel bound their SIDE-connection dial at a fixed 2 s (that half mattered more: `ferrod` awaits `cancel()` inline, so an unreachable backend stalled the teardown the deadline started). SPEC §22.2 (aj). ~~TCP keepalive on established connections~~ **CLOSED as WONTFIX-as-code, and the item was partly stale** (§22.2 (au)): measured, PostgreSQL defaults keepalive ON at 7200 s and MySQL defaults it OFF, so (aj)'s "keepalive is off" was half wrong; both are pool-DSN parameters needing no Ferro code, hardcoding one would remove an operator knob, and the hazard is already bounded by checkout `is_closed`/`ping`, `max_lifetime`, the mandatory per-request `timeout_ms`+`CANCEL`, and tx deadlines. ~~chunked `LARGE_OBJECT` bind~~ **SPLIT after verifying it**: the small half (B6a) is DONE — the PHP client enforced `MAX_FRAME_PAYLOAD` on DECODE only, so it would emit a frame its own decoder rejects, and since `ferrod` (correctly) treats an oversize `payload_len` as FATAL, an oversize bind KILLED THE SESSION rather than failing locally; the guard now mirrors Rust's and fires before any byte reaches the transport (SPEC §22.2 (ak)). The real chunked-bind half (B6b) is DEFERRED to M2 — it is a `/proto` design change plus an engine reassembly path, not a loose end. Next, and the tracker-clean hygiene `None`-skip (R2 — still blocked: hygiene is currently what masks the S8a Task 13 isolation leak). **Phase B is CLOSED (2026-09-10)** — delivered B1a/B1b, B2 (six slices), B3, B5, B6a; deliberately not done: B4 (WONTFIX — already solved, and the proposed fix had been rejected in writing by M1-S8a), B6b (chunked `LARGE_OBJECT`, a real `/proto` slice, deferred to M2), B7 (deferred on SPEC §7.2's own condition — the tracker's in-proc coverage across all reuse-relevant session variables is not yet proven).

Now **M2 / Phase C (Eloquent, SQLite backend, observability, DBAL ^3.8 bridge)**. **C1b has landed: `php/laravel` exists and executes** — `FerroConnections::register()` wires `ferro-pgsql` through Illuminate's `Connection::resolverFor()`, and `FerroPostgresConnection extends PostgresConnection` overriding `select()` ONLY, routed through `run()` so the query log, `QueryExecuted` event and `QueryException` wrapping are inherited rather than reimplemented. Stock Grammar/Processor/Schema untouched (charter rule 6). **`select()` is fate-declared a WRITE**, because `PostgresProcessor::processInsertGetId` runs `insert … returning id` through `selectFromWriteConnection()` — verified in v11.51.0 — so treating `select()` as a read would mis-declare the commonest Eloquent write on PostgreSQL. **C1c has landed too: writes and transactions.** `statement()`/`affectingStatement()`/`unprepared()` run over `fetch:none` with `affected` read from the engine rather than counted; transactions run over a minimal `FerroPdoShim`, which turned out to be the natural seam rather than a later convenience — `ManagesTransactions` is written entirely against `getPdo()`, so five shimmed methods inherit Laravel's transaction counter, savepoint naming, events and `attempts:` retry loop unchanged. **`FerroQueryException` puts SQLSTATE in `getCode()` (PDO's convention, the opposite of the Doctrine tier's errno convention), and that is mutation-proven live**: with the sibling's convention a real PG `40001` propagates out of `transaction(attempts: 3)` instead of retrying. **C1d has landed: `cursor()`/`LazyCollection` streams.** It was small as scoped — `streamRaw()` has streamed on both families since B2 — so this is tier wiring: `cursor()` is itself a Generator (as stock Illuminate's is), keeping the body lazy, and a `finally` drives `RawStream::close()` for the `CANCEL`+drain on abandonment. **The abandonment guard asserts the NEXT query**, because a missing cancel damages the following request rather than the abandoned one; mutation-proven, and without the `finally` the session HANGS behind unread frames rather than returning wrong data. Non-buffering is measured (200 000 rows under a 16 MiB bound). Only the rest of the PDO surface (C1e) remains, and it is half-built already. **C2's runner has landed and the first numbers are recorded** (`docs/laravel-suite/2026-09-10-c2-results.md`): the curated `laravel/framework` v11.51.0 integration subset runs against PostgreSQL through one `ferrod`, with the same hard contact assertion + reset discipline as the DBAL lane, and **two columns** — **65/71 under `ferro-pgsql`** (§15's one-word config change) and **71/71 — a clean subset — under the new opt-in `pgsql` alias** — each reproducible across two runs with identical ordered failure sets. Two columns rather than one because Illuminate resolves by driver NAME and six of upstream's own tests gate their assertions on `in_array($this->driver, ['pgsql','sqlsrv'])`; running the identical code under the alias turns exactly those six green, which is the proof rather than the assumption (§22.2 (am)). **The framework's own suite immediately found two tier defects that this package's unit and live tests were structurally blind to** (§22.2 (al)): all three execution paths skipped Illuminate's `prepareBindings()`, so a plain `Model::create(['created_at' => $carbon])` reached the engine tagged `TIMESTAMPTZ` and was correctly refused against the naive column `$table->timestamps()` creates (71 errors → 10); and the tier handed Illuminate the SPEC §9 value objects where PDO hands up driver-native strings, which `Builder::pluck($col, $key)` breaks on outright (`Cannot access offset of type Ferro\NaiveTimestamp on array`) — now `RawStringValuePolicy`, the same hand-off the Doctrine tier takes, with ONE deliberate divergence from PDO kept because it is safer (canonical RFC3339 `TIMESTAMPTZ`, which Illuminate's `Date::parse` fallback reads as UTC, where PDO's `+00` form is silently reinterpreted in the app timezone). **Every non-pass is triaged and none is a tier defect.** The one engine gap C2 measured — `I64` could not bind PG `float8` or `numeric` (Laravel's `$table->float()` is `double precision`; `extract(year from …)` is `numeric` on PG 14+) — **is CLOSED as of C2b** (§22.2 (an)): the mirror of M1-S9's `I64 → text`/`bool` widening, with `numeric` needing no value gate (arbitrary-precision, exact at every magnitude) and `float8` gated on exact representability and refused pre-send otherwise, deliberately stricter than `pdo_pgsql`, which rounds silently. `float4` was NOT widened — no measured caller, per (af)'s membership rule. The gate compares through `i128` because Rust's float→int `as` cast saturates and the obvious `(n as f64) as i64 == n` reports TRUE for `i64::MAX`. The remaining 6 are the driver-name artifact alone. **C2d then closed the last two PG bind gaps, found by probing a WIDER slice of the same upstream tree** (§22.2 (ap)): `I64 → varchar` (an int into a pivot's `$table->string()`) and `TEXT → int2/int4/int8` (a pivot key that round-tripped through PHP as a string) — mirrors of each other, both ordinary Eloquent. `bpchar` stays out (`char(n)` pads, so a read-back would not equal the write); `bool`, the float widths and `bytea` stay out of the TEXT arm because each would assign meaning or lose precision rather than merely render; and a non-numeric string is deliberately left to PostgreSQL's own `22P02` rather than pre-refused — known-fate, and what libpq does. **C2e then widened the allowlist from 5 files to all 89 driver-agnostic ones — 71 → 633 cases — and added a CONTROL column** (§22.2 (ar)): `FERRO_LARAVEL_DRIVER=stock-pgsql` runs the identical tests against the identical server through upstream's own `pdo_pgsql`, with the contact assertion INVERTED (it refuses to run if the connection turns out to be a Ferro one), mutation-proven twice. **The result is the headline: under the stock driver name Ferro is INDISTINGUISHABLE from `pdo_pgsql` — 605/607 on both, identical ordered failure sets, and the two remaining reproduce through stock PDO, so they are upstream's own.** Under `ferro-pgsql` it is 570/579, and every one of the nine differences is positively identified in upstream source as code branching on `$this->driver` (an `expectException` never armed, a `match` picking the wrong expected type, a `markTestSkipped` that never fires) — (am)'s cost restated with a control behind it rather than an argument. The widening also settled `search_path` (§22.2 (aq)): it belongs on the ferrod pool DSN, because the session is pooled — and Laravel needs it in the config array TOO, since `PostgresBuilder::getSchemas()` reads it from there and never asks the server. **C2f then ran upstream's PostgreSQL-SPECIFIC directory as its OWN column** (it has no `ferro-pgsql` row by construction — its base class carries `#[RequiresDatabase('pgsql')]`), and the control made its one failure attributable immediately: **`DB::escape()`**. `FerroPdoShim` refused `quote()` by name on the sibling tier's reasoning — "security-critical code with no known caller" — and the caller turned out to be ordinary, since `Grammar::substituteBindingsIntoRawSql()` reaches it, i.e. every `toRawSql()` and every `->dd()`. It is implemented (§22.2 (as)): Illuminate rejects NUL bytes and invalid UTF-8 first, so the rule is doubling `'` and nothing else — **verified, not assumed**, and measured byte-identical to `pdo_pgsql` over eight cases with a `SELECT <literal>` round trip. **C2f verified it the wrong way and C2g fixed that** (§22.2 (at)): the first version used a cached `SHOW standard_conforming_strings`, and §21 **D5** — read only afterwards, which is the actual defect — says `quote()` is client-side with **no engine round trip**. Charter ground rule 1 makes §21 binding and says an impossible decision is to be RAISED, not routed around; this one was not even impossible. `HELLO_ACK` now advertises **`literals_are_standard`** per pool at `protocol_version` **3**, learned from a `GUC_REPORT` `ParameterStatus` at zero cost and riding the same per-pool probe as `server_version`, so quoting sends nothing (asserted on the session's own sent-frame record). The field is deliberately NOT named for either backend's setting — MySQL's equivalent is `NO_BACKSLASH_ESCAPES`, and MySQL needs the bit more than PG does since it defaults to the unsafe value — and `nil` is the fail-closed arm the shim refuses on, never read as false or true. That column is now **74/74, identical to the control down to the assertion count.** **The bar is NOT met and the doc says so**: MySQL is not run (the tier registers `ferro-pgsql` only), and SQLite is unreachable until C3. Scope and premises — see `docs/dev-loop/PHASE-C-SCOPE.md` for the C1a–C1e slice plan with each premise marked verified or UNVERIFIED. Most of what the Illuminate tier needs from below is already paid (notably `streamRaw()` streams on both families since B2, so `cursor()`/`LazyCollection` is cheap). **C1a was found and decided while scoping:** `selectResultSets()` has no wire representation (`ExecOk` carries exactly one `cols`+`rows`), but that is not its real blocker — a MySQL `CALL` returned no usable rows at the time, because a prepared `CALL` declares ZERO result columns even when the procedure emits a result set (streamed → rows discarded; buffered → N cell-less rows) — **that half is FIXED** (§22.2 (av) buffered, (aw) streamed: `cols` falls back to the EXECUTED result set's metadata), and only the `/proto` multi-result-set change remains deferred, on the separate ground that no tier can reach a second result set. A stored procedure is what `selectResultSets()` is for, so a `/proto` multi-result-set change would ship a feature that still returns nothing. It is a documented incompatibility for M2; the CALL blind spot is the true prerequisite, and it is engine work. **C2 (the M2 acceptance bar) is scoped and part-provisioned.** Measured against upstream SOURCE rather than assumed: it is materially HEAVIER than the DBAL suite and is at least two slices, because every test extends `Orchestra\Testbench\TestCase` — a real `Application` + console `Kernel` — and `DatabaseMigrations` drives `artisan migrate:fresh`, so it needs `orchestra/testbench-core` as a new dependency and working Artisan/DDL. **The silent-SQLite trap exists here too** (testbench's `LoadConfiguration::bootstrap()` injects an in-memory SQLite `testing` connection unconditionally, and upstream's `phpunit.xml.dist` points `DB_CONNECTION` straight at it) — but it keys on the NAME `testing`, and an unconfigured custom name throws loudly, so it is avoidable by construction. Landed so far: the `laravel_tests` database and its container-side reset. §15's acceptance bar names SQLite, which stays unreachable until the SQLite backend (C3) lands. The ORM tier still needs its SEQUENCE-strategy documentation for PostgreSQL. **The bar is NOT met and the doc says so**: MySQL is not run (the tier registers `ferro-pgsql` only), SQLite is unreachable until C3, and 71 tests is a curated subset of upstream's ~656. **C2c fixed a defect the wider suite then found, and it is the sharpest lesson of the phase** (§22.2 (ao)): the tier handed Illuminate the RAW `version()` banner, and PHP's `version_compare` reads `PostgreSQL 16.13 (…)` as OLDER than everything — silently, no throw, no warning — so stock `PostgresGrammar::compileColumns()` emitted its pre-12 introspection SQL against a modern server and `Schema::getColumns()` reported every column as non-generated. `FerroPdoShim::getAttribute`'s own docblock had named that exact hazard while triggering it: the nil-version guard was right and incomplete, because a version that is PRESENT but unparseable is just as wrong and much quieter. Fixed by reusing the sibling Doctrine tier's rule unchanged (strip PostgreSQL's leading product name, pass the MySQL family byte-identical so MariaDB stays detectable), with the live guard asserting the CONSEQUENCE — a `GENERATED ALWAYS AS … STORED` column must come back reported as generated — rather than the number. Scope and premises — see `docs/dev-loop/PHASE-C-SCOPE.md` for the C1a–C1e slice plan with each premise marked verified or UNVERIFIED. Most of what the Illuminate tier needs from below is already paid (notably `streamRaw()` streams on both families since B2, so `cursor()`/`LazyCollection` is cheap). **C1a was found and decided while scoping:** `selectResultSets()` has no wire representation (`ExecOk` carries exactly one `cols`+`rows`), but that is not its real blocker — a MySQL `CALL` returned no usable rows at the time, because a prepared `CALL` declares ZERO result columns even when the procedure emits a result set (streamed → rows discarded; buffered → N cell-less rows) — **that half is FIXED** (§22.2 (av) buffered, (aw) streamed: `cols` falls back to the EXECUTED result set's metadata), and only the `/proto` multi-result-set change remains deferred, on the separate ground that no tier can reach a second result set. A stored procedure is what `selectResultSets()` is for, so a `/proto` multi-result-set change would ship a feature that still returns nothing. It is a documented incompatibility for M2; the CALL blind spot is the true prerequisite, and it is engine work. **C2 (the M2 acceptance bar) is scoped and part-provisioned.** Measured against upstream SOURCE rather than assumed: it is materially HEAVIER than the DBAL suite and is at least two slices, because every test extends `Orchestra\Testbench\TestCase` — a real `Application` + console `Kernel` — and `DatabaseMigrations` drives `artisan migrate:fresh`, so it needs `orchestra/testbench-core` as a new dependency and working Artisan/DDL. **The silent-SQLite trap exists here too** (testbench's `LoadConfiguration::bootstrap()` injects an in-memory SQLite `testing` connection unconditionally, and upstream's `phpunit.xml.dist` points `DB_CONNECTION` straight at it) — but it keys on the NAME `testing`, and an unconfigured custom name throws loudly, so it is avoidable by construction. Landed so far: the `laravel_tests` database and its container-side reset. §15's acceptance bar names SQLite, which stays unreachable until the SQLite backend (C3) lands. The ORM tier still needs its SEQUENCE-strategy documentation for PostgreSQL. **C3's gate is now OPEN (2026-09-14): the §7.6 tension is SETTLED as SPEC D13.** §7.6 asked for "one writer serialized engine-side", which requires identifying writers — the read/write inference charter rule 6 and §3 forbid, and which §1's own bullet already forbade for the neighbouring replica case *while pointing at §7.6 as the explicit-routing authority*, so the spec contradicted itself across two sections. Raised under ground rule 1 rather than resolved in code. **D13 is a DECLARATION rule:** every SQLite transaction takes the writer lock at `BEGIN`; one the client DECLARED `readonly` takes a deferred reader lock; an UNDECLARED statement is a writer — conservative, never wrong, the same trade §22.2 (ac) records. The cheapest option (`busy_timeout` alone) was rejected as a design because it leaves the deferred-upgrade case (`SQLITE_BUSY_SNAPSHOT`, which `busy_timeout` does NOT retry) needing the rollback-and-replay rule 3 forbids the engine performing — its WAL/`busy_timeout` pragmas are kept regardless, and that premise is still marked UNVERIFIED and must be REPRODUCED before the first code slice precisely because it is the rejected option's crux. **What it bought beyond SQLite:** half of D13 is nearly free (the TX service already composes BEGIN from `readonly`; PG already emits `START TRANSACTION READ ONLY`), but the other half is a new seam — `readonly` reaches `ferrod`'s SQL service and NOT `ferro-pool` — and that same `Pool::checkout` seam is what single-flight read coalescing and read-your-writes replica routing would each need, making D13 the first decision that makes an honest `readonly` declaration pay for itself. §7.6, §21 D13, §22.2 (az), `docs/dev-loop/C3-SQLITE-SCOPE.md`. **C3-1 (the premise spike) is DONE and ALL FOUR PREMISES HOLD**, so C3-2 is unblocked (§22.2 (ba); `engine/crates/ferro-sqlite-spike/` — a spike crate that ships no library code and is NOT `ferro-backend-sqlite`, which still does not exist). No vendored driver fork is needed *for the capabilities the seam demands* — stated that way because "no fork will ever be needed" is not checkable, and this is the assumption proven FALSE for `mysql_async` at S6 and `tokio-postgres` at S1, both times before any code; rows stream across `spawn_blocking` without buffering AND the `Connection` comes back usable, so the B2b-2a park/unpark shape and the `reclaim_stream` seam both fit; `InterruptHandle` is `Send + Sync + 'static` (compile-time) and really interrupts (run-time). **D13's required `SQLITE_BUSY_SNAPSHOT` reproduction is discharged:** a deferred upgrade fails 517 in under 500 ms **with a 5-second `busy_timeout` armed on the failing connection**, paired with a CONTROL where lock-at-BEGIN yields a plain `SQLITE_BUSY` (5) after the timeout parks the full duration — without that control the timing assertion would be equally consistent with "the timeout was never armed". **SQLite is the first backend whose spike needs no server**, so every gate ran locally and CI is not the authority for any of it. Two findings carried forward: the first non-buffering proof **asserted nothing** (the unbounded-channel mutation PASSED — fixed with a stall probe, after which it fails with exactly the predicted count), and **`ffi::ErrorCode::X as i32` is not SQLite's code and compiles cleanly** (`DatabaseBusy` 3 vs `SQLITE_BUSY` 5; `OperationInterrupted` 7 vs `SQLITE_INTERRUPT` 9), which C3-2's `error_map` will key on. Left open for C3-2: §7.1 says "protocol signals" throughout while SQLite's pin authority is a library call — it fits the seam but is a different MECHANISM, so §7.1 needs amending. **C3-2 is DONE: the `compose_begin_sql` SQLite arm** (§22.2 (bb)). The placeholder it replaced was not merely incomplete but WRONG — it emitted a bare `"BEGIN"`, which SQLite reads as DEFERRED, i.e. exactly the deferred-upgrade shape `p4a` proved is unretryable; the arm meant to stop a future backend inheriting PG syntax was itself reintroducing the class D13 exists to remove. Now `BEGIN IMMEDIATE` (undeclared or declared-write) / `BEGIN DEFERRED` (declared-readonly), mutation-proven both ways. **Isolation is accepted and emits nothing — a decision, not an oversight:** SQLite has no `SET TRANSACTION`, is serializable, and SERIALIZABLE is stronger than either weaker level, so every level a client can name is already satisfied; under D13 `readonly` alone decides the lock mode, so the level cannot change blocking behaviour either, and refusing would break drop-in for an app that merely configures one. An unknown isolation BYTE is still a client error. **A fifth premise (`p5`) was proven BEFORE this arm shipped, because the arm creates the hazard:** D13's readonly arm hands the client `p4a`'s exact setup, so a lying `readonly` declaration reaches the one failure class charter rule 3 forbids the engine resolving — `PRAGMA query_only=ON` converts it to an up-front `SQLITE_READONLY` (8), deterministic and provably not executed, proven on `p4a`'s own fixture and with no contention at all. **Scope-doc §6 was renumbered** (it predates D13): C3-3 is now the `readonly`/`Pool::checkout` seam and the backend crate is C3-4, **which owes the `query_only` pragma and a live lockstep test** — nothing currently links the engine's emitted string to `p4b`'s proof that the string takes the writer lock. `Dialect::Sqlite` is unreachable at runtime until a SQLite pool exists, so a unit test is the only gate C3-2 could honestly offer. **C3-3 planning (2026-09-15) produced no backend code, and that was the right call** (§22.2 (bc)). The `readonly`/`Pool::checkout` seam had been scheduled next; `readonly` genuinely does not reach `ferro-pool` (it occurs there exactly twice, both comments), but **no backend would have READ it** — PG has nowhere to apply a per-statement readonly outside a transaction, MySQL likewise, and the SQLite backend does not exist — so building it first meant a parameter nothing consumes, tested only for arriving. It is now C3-4, after the backend that gives it a consumer. Separately, "the backend crate" was one undifferentiated item against measured precedents of **5623 lines (PG) and 4040 (MySQL)**; it is now split **C3-3a–e** by what can be tested without the next piece existing, with the `AnyPool` arm LAST so nothing before it changes `ferrod`. **§7.1 is AMENDED:** it said pin decisions come from "backend protocol signals" and had no SQLite paragraph for pin AUTHORITY at all — SQLite's authority is `sqlite3_get_autocommit()` via `Connection::is_autocommit()`, synchronous and round-trip-free, satisfying the real invariant (*authority from the backend, never inference from statement text*), so the framing generalises rather than carving an exception. It rests on a NEW premise, not reasoning: **`p6`** proves the signal must be read after every statement rather than tracked, because SQLite ends transactions by itself — an `ON CONFLICT ROLLBACK` violation rolls the whole transaction back with nothing in the SQL text saying so, while the CONTROL (a plain unique constraint, default `ON CONFLICT ABORT`) fails only the statement and leaves the signal in-transaction. Mutation-proven. That premise is load-bearing for C3-3b: `tx_status` must be a live read, never a cached flag. **C3-3a is DONE: `engine/crates/ferro-backend-sqlite` EXISTS** (§22.2 (bd)) — crate, `SqliteConn`, connection setup and `connect`/`ping`/`is_closed`/`dialect`, separate from the spike (which stays the premises suite). **No `impl PoolBackend` yet, deliberately:** 8 of the trait's 12 methods would be `Unsupported` stubs, and a stub returning `Unsupported` is indistinguishable at the type level from a finished method, so the incompleteness would stop being visible exactly when it matters. **The park/unpark bridge is the load-bearing part** — `rusqlite` is sync, the trait async, and `Connection` is `Send` but not `Sync`, so it is moved into `spawn_blocking` and back (`p2`'s shape, and `MysqlConn`'s); every later slice's statement runner goes through it. **WAL is VERIFIED not assumed** (the pragma returns the mode in force and SQLite can decline it) — though the guard's failure arm is not reachable in this container, so the test proves the connection is in WAL rather than exercising the guard. **An in-memory DSN is REFUSED with the hazard demonstrated first:** two `:memory:` connections cannot see each other's table, so a pool would silently hand different tenants different databases. **Two pieces of speculative state were removed on review** — an unobservable `dead` flag, and a `lose_handle()` call in the panic arm that a mutation exposed as DEAD CODE (deleting it left the test green, because `park` had already moved the handle out): the contract holds by construction, and a green test is not evidence the code beneath it does anything. **Open, not decided:** SQLite defaults `foreign_keys` OFF while Laravel/Doctrine turn it ON — left to C3-6 with suite evidence. **Wiring debt:** busy_timeout defaults to `PoolConfig`'s default `checkout_timeout`; C3-3e must pass the real one. **C3-3b is DONE: `tx_status`, `reset`/`clean_reset_profile`, `simple_query`** (§22.2 (be)). **`tx_status` is a LIVE read** and `p6`'s hazard is proven through the real backend with its control — an `ON CONFLICT ROLLBACK` violation ends the transaction underneath the engine and the signal reports `Idle`, while a PLAIN unique constraint fails only the statement and stays `InTx`; mutation-proven against a cached flag. `Failed` is returned in exactly one case, a connection with no live handle, which is the ABSENCE of a signal — **§7.1 was amended to say that precisely.** **BOTH of SQLite's row counters are wrong on their own, measured not reasoned:** `changes()` goes STALE (3 rows inserted, then `BEGIN` still reports 3 — and `simple_query` is the path the pin hook runs BEGIN/COMMIT/ROLLBACK through), while `total_changes()` OVER-reports (an `AFTER INSERT` trigger moves it by 2 for a 1-row insert; an `ON DELETE CASCADE` with two children by 3 for a 1-row delete). So the `total_changes()` delta decides WHETHER anything changed and `changes()` reports HOW MANY, matching what PG and MySQL report; both failure modes mutation-proven. Trade-off: a batch reports its last statement's count, as PG/MySQL also do. **`reset` is an explicit 4-item list** (open transaction, `query_only`, ATTACHed databases, TEMP objects — which live in the per-connection `temp` schema and outlive the checkout) because SQLite has no `DISCARD ALL` to port; both profiles shipped identical, with `clean_reset_profile` at `Some(Full)` — **both of those were corrected on 2026-09-15, see §22.2 (bm)**: the list was derived from what the ENGINE leaves behind and a tenant's `PRAGMA` survives all four of them. **`impl PoolBackend` is still absent but the line is now drawn at C3-3d**, when only the streaming pair would be `Unsupported` — the shape MySQL shipped at M1-S6. **C3-3c is DONE: `query` + the row/value mapping** (§22.2 (bf)). **It keys on the VALUE's storage class**, because SQLite's declared column type is only an unenforced AFFINITY — measured: a column declared `INTEGER` returns INTEGER in row 1 and TEXT in row 2, and every EXPRESSION column (`1+1`, `count(*)`) reports no declared type at all. **The objection — that per-value tagging makes the `cols` header a lie — DISSOLVES, because `ColMeta.tag` has no consumer:** the PHP client drops it deliberately in both paths (F25/hazard 47: "the decode authority is the PER-CELL tag"), and nothing in `ferrod` reads it. That corrects the framing the slice was handed, and checking for a consumer first is the discipline that stopped C3-3 building a seam nothing would read. **NINE of the 14 §9 tags are unreachable BY CONSTRUCTION** — `BOOL`, `U64`, `DECIMAL`, the four date/time tags, `UUID`, `JSON` — named rather than given an invented mapping (S7's `UUID` precedent). **Drop-in consequence for C3-6:** an ISO date in a SQLite column reads back `TEXT`, not a §9 `Date`, as PDO's SQLite driver also does. BIND is deliberately asymmetric (all 14 bind; canonical text lands in TEXT), with `U64` above `i64::MAX` refused pre-send rather than wrapped. **`last_insert_rowid()` is sticky exactly like `changes()`** and is gated the same way — reported only when the statement moved it, since a carried-over key would be silently WRONG and §22.2 (m) already records that as worse than none. **C3-3d is DONE: `cancel_handle`, the `error_map` fate table and `impl PoolBackend`** (§22.2 (bg)) — the trait landed exactly where C3-3b drew the line, with only the STREAMING pair `Unsupported`; `SqliteRowStream` wraps `Infallible`, so that impossibility is enforced by the TYPE SYSTEM rather than by an `unimplemented!()` nobody has run. **SQLite is the mirror image of PostgreSQL on error identity** — an errno and no SQLSTATE, where PG has a SQLSTATE and no errno and MySQL has both — so `sqlstate` is `None` and `errno` carries the EXTENDED code, which is what separates `SQLITE_BUSY` (5) from `SQLITE_BUSY_SNAPSHOT` (517); every constant is asserted against an error a real SQLite produced, with a companion test for the C3-1 trap that `ErrorCode::DatabaseBusy as i32` is 3, not 5. **Both BUSY codes classify Retryable, reversing this slice's own instruction:** 517's "statement re-send is futile, transaction replay works" is precisely PG's `40001`, which the engine has classified Retryable since M1-S4, and `Branch`'s doc settles it (*"Retryable only licenses the CALLER to retry per its own policy"*); under D13 a 517 should be unreachable, so reaching it means a declared-`readonly` transaction wrote, and the message says exactly that. **The finding that outlives the slice: `tokio::time::timeout` CANNOT rescue a runaway `spawn_blocking` statement** — the mutation hung rather than failing — so the interrupt handle is the ONLY mechanism that can stop a statement on this backend, and `ferrod` must FIRE the cancel, not merely abandon the future. `cancel_handle` bounds nothing deliberately: PG and MySQL bound theirs at 2 s because each opens a side connection (§22.2 (aj)), while `sqlite3_interrupt()` sets an in-process flag on a handle already owned. **C3-3e is DONE: the third `AnyPool` arm** (§22.2 (bh)) — `sqlite://` infers `PoolKind::Sqlite`, the registry builds `AnyPool::Sqlite(Pool<SqliteBackend>)`, and the SQL/TX services dispatch to the SAME generic bodies (`run_exec_on_pool`/`begin_on_pool`) they already had: **nothing about the registry's shape changed to take a backend that is a synchronous LIBRARY rather than a wire protocol**, which is the check the `PoolBackend` seam had to pass and had never faced. A bare filesystem path is deliberately NOT inferred as SQLite (declaration over inference — it would swallow the typo'd DSNs the warn arm exists to surface), so under `ferrod` a SQLite pool is spelled `sqlite://…`. **Both carried debts are discharged:** the pool's `checkout_timeout` is now the connection's `busy_timeout` (they fail in opposite directions if they disagree; proven behaviourally at 250 ms against the 5 s default, because the registry-level equality assertion is VACUOUS while both constants are 5 s — stated in the test rather than left to be discovered, with a per-pool knob recorded as the real fix); and the C3-2 lockstep landed. **A premise carried since M1-S6 measured FALSE:** `VERSION_SQL`'s "no per-backend method is needed" — SQLite has no `version()` at all, so the probe is per-family now, held in a separate tuning field so BOTH statements stay test-steerable, with the control re-running the identical registry under `SELECT version()`. **THE LOCKSTEP TEST WAS GREEN UNDER THE MUTATION IT EXISTED TO CATCH**, and that is the slice's lesson: a write inside a DEFERRED transaction UPGRADES it, so after any statement both spellings hold the writer lock — D13's claim is that the lock is taken AT BEGIN, visible only between BEGIN and the first statement, which is where the side connection now looks; both arms mutation-proven after the correction. Also found by a failing test: **`PRAGMA journal_mode=WAL` cannot switch while another connection holds a lock**, so a SQLite pool's first connection must not be raced against an external writer. **And the pre-push diff re-read caught a defect the arm MADE REACHABLE:** `supports_row_streaming` defaults to `true` and `SqliteBackend` was inheriting it while `query_stream` is `Unsupported` until C3-5 — `ferrod` reads that one method to refuse a `fetch:stream` EARLY, so the default would have turned a clean refusal into a mid-stream error the moment a SQLite pool became constructible; now overridden to `false` with the pairing mutation-proven. **C3-4 is DONE: the `readonly`/`Pool::checkout` seam** (§22.2 (bi)) — `Pool::checkout_declared(readonly)` carries the client's DECLARED flag to a new `PoolBackend::apply_readonly`, which SQLite implements as `PRAGMA query_only`, giving C3-3a's `set_query_only` its first caller; `checkout()` delegates with `false`, so every existing caller is unchanged (the M1-S6 `take_session_mutated` shape). It is a declaration HONOURED, never an inference: the same flag is already trusted by `fate.rs` to suppress `Indeterminate` and by `compose_begin_sql` to choose `BEGIN DEFERRED`, so a lying declaration steers the engine into `p4a`'s unretryable class — the pragma turns it into an up-front `SQLITE_READONLY` (8). **THREE `ferrod` call sites, not two** (buffered exec, STREAM, tx BEGIN — the M1-S8b `setTransactionIsolation` shape), and **both checkout exits**, the recycled one AFTER hygiene, each mutation-proven in isolation — enforcement on one exit alone would depend on POOL OCCUPANCY. **TWO of this slice's own tests were proven WRONG by the mutation round, which is the slice's real value:** one claimed to exercise the fresh-dial exit through `ferrod` and cannot (HELLO's `pool_info` version probe pools a connection, so every daemon EXEC takes the recycled exit — the proof moved to pool level); the other claimed hygiene's `query_only=OFF` disarms between tenants, and deleting that line left it GREEN — **and the first explanation for THAT was wrong too**: the test had stopped testing recycling, because its middle step ends in a REFUSED statement and a failed connection is not the one the next checkout gets, so the final step was silently served a fresh dial. One SUCCEEDING readonly checkout in between makes the mutation fail it. **Hygiene IS what disarms a recycled connection and the seam cannot**, because `reset` clears `apply_readonly`'s tracked flag unconditionally, so afterwards the flag reads off whether or not the pragma is and `apply_readonly(false)` short-circuits. **The reusable lesson is about the POOL, not SQLite: a setup step ending in a failed statement converts a recycle test into a fresh-dial test.** **Recorded not built:** PG's session-scoped `default_transaction_read_only`, armed per checkout and cleared by the existing hygiene `RESET ALL`, would let the PG arm enforce the same declaration — the first thing since §22.2 (ac) that would make an honest `readonly` cost something on PostgreSQL. **C3-5 is DONE: `query_stream` + `reclaim_stream`, and the SQLite backend is now COMPLETE against `PoolBackend` with nothing `Unsupported`** (§22.2 (bj)). The capability and the implementation flipped in ONE change as (bh) required, and C3-3d's "streaming is the only `Unsupported` left" test was UPDATED rather than deleted. **SQLite is conn-owning in MySQL's exact sense and not by choice** — `Statement`/`Rows` borrow the `Connection`, which is `Send` but not `Sync`, so the stream owns it on one blocking thread for its life (hence `reclaim_stream`, and hence counters read from the CONNECTION post-drain, since `changes()` goes stale). **THE NON-BUFFERING TEST WAS WORTHLESS TWICE, both times the same failure:** v1 (take rows, sleep, assert the next follows) holds identically when the producer buffered everything, so it passed under its own mutation — `p2`'s lesson arriving again on a test written KNOWING it; v2 added an interrupt but at 200 000 rows a widened producer only reaches ~157 000 in 250 ms, so the interrupt truncated either way. Calibrated by measurement to 50 000 / 400 ms, the mutation now fails with exactly `seen == 50000`. **The general rule: a stall probe is a proof only when what it measures cannot ALSO be produced by the mutated code being merely SLOW.** C3-3a's removed `dead` flag STAYS removed on C3-3a's own condition — checked, the parked window is not pool-visible, because the connection is checked out between `query_stream` and `reclaim_stream`. **C3-6a is DONE: the Doctrine tier learns the SQLite family, and the DBAL subset has a SQLite column WITH a control** (§22.2 (bl); `docs/dbal-suite/2026-09-15-c3-6a-sqlite-results.md`). **329 passed / 357 executed through Ferro against a CONTROL at 357/357** — upstream's own `pdo_sqlite` on the SAME database file with the contact assertion INVERTED (the C2e shape), two runs per column with identical result lines and ordered failure sets, and three guards mutation-proven (the control pointed at Ferro is refused; the Ferro column pointed at `pdo_sqlite` is refused; setting both `db_driver` and `db_driverClass` is refused, because `DriverManager` silently prefers `driverClass` and such a "control" would have been measuring Ferro). **The measurement was built FIRST and it named the work** — the tier got only the minimum that lets a connection open, then the failures said what else was needed. The tier change is mostly delegation: `KIND_SQLITE` with an `AbstractSQLiteDriver` delegate (which ignores the version — there is one SQLite platform), a bind-side-only `TemporalFormat` arm (no SQLite backend ever EMITS a temporal tag), the stock exception table — which keys on the **MESSAGE TEXT**, so the arm works only because `error_map` passes SQLite's own message through unaltered — and a third `lastInsertId` message. **`familyFromVersion()` deliberately does NOT learn SQLite**: a bare `3.53.2` and a bare `8.4.11` are indistinguishable, so both take the null arm and the platform-before-connect path fails loudly (the rule, already true of MySQL: do not set `serverVersion`). **SQLite is the first family whose whole acceptance column needs no server**, so the runner's reset is a file delete — with the `-wal`/`-shm` sidecars, and BEFORE `ferrod` opens the file, which is why the reset became a function with two explicit call sites. **All 28 non-passes have two causes and neither is a driver defect — and the big one is §7.4, not SQLite.** 24 are DBAL's `__temp__` table rebuild, whose five statements run OUTSIDE a transaction while a TEMP table cannot survive between checkouts; **measured, PostgreSQL loses an autocommit TEMP table identically** and both families keep it inside one transaction, so SQLite is merely the first family whose STOCK Doctrine schema manager depends on session state between statements, and the remedy — wrap the ALTER in a transaction — is VERIFIED rather than proposed. The other 4 send two statements in one `executeStatement()`, which **PostgreSQL refuses identically** (`42601`): one statement per request is a uniform contract. **`foreign_keys` resolved OPPOSITE to C3-3a's premise: it was already ON, and nobody had decided it** — `rusqlite`'s `bundled` feature compiles libsqlite3-sys with `SQLITE_DEFAULT_FOREIGN_KEYS=1`, where `pdo_sqlite` reports 0. An integrity guarantee resting on a dependency's compile flag is now SET and READ BACK at dial, with a tripwire test that asserts the raw build default and says out loud that the pragma is currently redundant. Beside it, a real finding: **neither Doctrine's `EnableForeignKeys` middleware nor Laravel's connector can work here**, because both apply a SESSION pragma at driver-connect — the guarantee has to come from the pool, exactly as MySQL's `time_zone = '+00:00'` does. **The dates-read-back-as-`TEXT` asymmetry costs the Doctrine tier nothing** (every temporal row of `TypeConversionTest` passes; DBAL's types parse the platform's format strings out of strings) — re-ask it for Illuminate at C3-6b. **Not established, and the docs say so:** §14's bar stays unmet (329/357, ORM suite not run at all), and `php/doctrine-dbal`'s own live tier still has no SQLite lane. **Carried to C3-6b:** the Laravel tier registers `ferro-pgsql` only, so a SQLite column needs a `FerroSQLiteConnection`; and **`literals_are_standard` is nil on a SQLite pool**, which C2g's `FerroPdoShim::quote()` refuses on by design — so `DB::escape()`/`toRawSql()` will fail there until the backend advertises it. **A CROSS-TENANT SQLite SESSION LEAK was then found WHILE SCOPING C3-6b, and fixed instead** (§22.2 (bm)) — the iteration's actual deliverable. Probing how Laravel's `SQLiteBuilder::dropAllTables()` behaves through Ferro (it runs `PRAGMA writable_schema = 1` and then `DELETE FROM sqlite_master` as SEPARATE statements, which on a transaction-mode pool should not have worked) showed that it DID work: **SQLite pragmas are connection-scoped and C3-3b's 4-item reset cleared none of them, so 17 of 23 probed survived a recycle** — `foreign_keys=OFF` (silently disarming the guarantee C3-6a had just declared, for everyone after), `writable_schema=1` (letting the next tenant destroy the schema without asking), `trusted_schema`, `ignore_check_constraints`, `read_uncommitted`, `synchronous=0`/`fullfsync` (durability), `busy_timeout` (the pool's own bound), and eleven more. Reproduced first through two real PHP client sessions. **The fix is not a longer list** — SQLite has ~60 pragmas and gains more each release, so a hand-kept list rots silently, the exact failure C3-6a had just found in `foreign_keys` resting on a build flag. **`ResetProfile::Full` now closes the connection and opens a fresh one**, which is what `COM_RESET_CONNECTION` is for MySQL (M1-S6), is complete by construction, and re-applies the DECLARED setup (WAL verified, the pool's `busy_timeout`, `foreign_keys` on and read back) rather than trusting the last tenant. **`clean_reset_profile` moved to `Some(Targeted)`** because a reopen measured ~250 µs against §16's p50 < 60 µs target; narrowing is safe because `PRAGMA`/`ATTACH` — the only statements that leave connection-scoped state — taint UNCONDITIONALLY in `classify_one_sqlite`, a load-bearing property that had no test and now has one with a control. **Nothing was weakened:** a clean connection gets exactly what it got before, a tainted one strictly more. **The sharpest test distinguishes a REOPEN from a SCRUB** — `last_insert_rowid()` is per-connection and sticky, so a scrubbed connection still answers the previous tenant's rowid and a reopened one answers 0; without it an implementation that enumerated a few pragmas would pass everything else while carrying the other fifty forward. Four mutations, each failing exactly the tests that claim it. **C3-6b is DONE (2026-09-15): the Illuminate tier learns SQLite, and the framework suite has a SQLite column with its control** (§22.2 (bn); `docs/laravel-suite/2026-09-15-c3-6b-sqlite-results.md`). **575 passed / 579 executed under `ferro-sqlite`, against a CONTROL at 586/588** — upstream's own `pdo_sqlite` opening the SAME file with NO ferrod started at all, which is stronger than pointing it elsewhere: a control with no Ferro to reach cannot quietly route through one. Two runs per column, outcome counts and ordered failure sets identical. **The measurement was built first and it named every piece of tier surface that followed**, four times, none designed for in advance: the first smoke run died 11/11 on `getPdo()->lastInsertId()`; the second case failed on `table "users" already exists`; the first FULL run reported **182 of 225 errors on one line**; 40 more were an alias hijack. **The execution layer was family-agnostic in exactly one character** — only `extends PostgresConnection` — so it is a TRAIT now rather than a shared base class, because the thing each family must NOT share is its parent (a common ancestor could only have been `Connection` itself, discarding the stock Grammar/Processor/Builder charter rule 6 keeps). **`lastInsertId()` is where the pooled engine and PDO genuinely disagree, and the disagreement is resolved in the PDO shim.** The client's value is per-STATEMENT and cleared on the way in to every request — right for the ENGINE, since a pooled statement can land on another backend connection and a carried-over rowid would be a silently wrong key — while PDO's belongs to the HANDLE. `Processor::processInsertGetId()` looks like it reads the id immediately, but `Connection::insert()` fires `QueryExecuted` first and any listener that runs a query clears it. So the CONNECTION remembers each non-null key from its own writes and never clears it; that value is what `pdo_sqlite` returns in every case, including after a rollback. Engine, client and the Doctrine tier untouched. The discriminating mutation is the NAIVE implementation (shim reads the client directly), which passes a plain insert-then-read test and fails only the two encoding PDO's stickiness. **`dropAllTables()` needed a schema-builder override, and the defect produced itself as an artifact:** during the mutation round, with the stock builder restored, the live tests left two empty files named `laravel_tests` and `ferro_sqlite_label` in the REPOSITORY ROOT — the config LABELS — while every table survived. `FerroSQLiteBuilder` takes the statement branch unconditionally, makes `refreshDatabaseFile()` THROW, runs the four stock-grammar statements in ONE transaction and `vacuum` after the commit; both halves mutation-proven with SQLite's own messages naming the mechanism. **On SQLite the contact assertion cannot be satisfied by the trap it exists for** — testbench's fallback IS SQLite, so "something answered and it said SQLite" proves nothing. The probe reads `PRAGMA database_list` and requires `main` to be open on the expected FILE, mutation-proven against a decoy path, against `:memory:`, and against the stock connection class. `sqlite_version()` replaces `version()`. Harness trap recorded: the path must be derived with the ENGINE's rule (`strip_prefix("sqlite://")`), because PHP's `parse_url()` returns FALSE outright for `sqlite:///abs/path`. **All four non-passes are triaged and only ONE is a real incompatibility.** The failure reproduces in the control. Two are the C2e driver-NAME artifact (`markTestSkipped` gated on `$this->driver === 'sqlite'`) — **and they SKIP in the alias column, which is the proof rather than the argument**. **The fourth is §7.4 again through a NEW mechanism, with three remedies ruled out BY MEASUREMENT:** `SQLiteGrammar::compileAlter()`'s six statements are six checkouts, so `PRAGMA foreign_keys = OFF` never reaches the `drop table` and the child row refuses it (errno 787). As Illuminate runs it → refused; **all six inside ONE transaction → still refused**, because the pragma is a no-op inside a transaction — exactly what makes this different from C3-6a's `__temp__` remedy and from `dropAllTables()`; transaction + `PRAGMA defer_foreign_keys` (transaction-legal) → still refused. No execution-layer remedy exists, and inventing one would mean substituting SQL the stock grammar did not emit. Recorded for C6. **The stock-name alias is NOT a drop-in escape hatch on SQLite the way it is on PostgreSQL:** upstream's own tests use throwaway SQLite connections regardless of the family under test, none carrying a `ferro_socket`, so the alias column costs 42 extra errors for the two name-gated skips it buys. The harness now registers the alias only in the column that actually RUNS under the stock name, and **the PostgreSQL column was re-run as a regression check with the same outcome counts as C2e's recorded one**. **Not established:** §15's bar still names MySQL, which this tier does not register at all, and the ORM suite is not run on any family. **C3-7a is DONE (2026-09-15) and C3-7 is SPLIT: the online-backup MECHANISM already works, so C3-7 is an ADMIN SERVICE slice** (§22.2 (bo); scope doc §21). Probed before building anything, the obvious reading — wire `sqlite3_backup_*` through a new engine method — is unnecessary: **`VACUUM INTO` is a PLAIN STATEMENT the pool already runs**, it is SQLite's own recommended live-snapshot mechanism, and §22.2 (bg)'s interrupt handle (the only thing that can stop a runaway SQLite statement) already bounds it; `sqlite3_backup_*` would additionally need a connection pair OUTSIDE the pool, the second lifetime the `PoolBackend` seam exists to avoid. **What C3-7 is actually missing is the transport, and it does not exist at all:** `ADMIN = 5` is a reserved `/proto` service id with NO `[methods.admin]` table, and `dispatch::route(service::ADMIN, _)` returns `Unsupported` with a test asserting it. **C3-7b is NOT started** and owes a charter-rule-2 `/proto` change, a dispatch route, an AUTHORIZATION model with no precedent in the tree, a destination policy, and a client/CLI surface — and §13's `ferro top` needs the same transport, so C3-7b opens it for C4 too. **Four properties measured rather than assumed:** no pool coordination is needed (a snapshot under a concurrent OPEN write transaction succeeds AND excludes that transaction's uncommitted row — the worry worth checking, since a snapshot reads pages while a writer holds the WAL); SQLite refuses an existing target, so "overwrite the last snapshot" needs a destructive delete that belongs to C3-7b's authorization; a refused snapshot leaves a ZERO-BYTE file, and the obvious follow-on worry is FALSE because that leftover does not block the retry; and **`PRAGMA query_only` bounds writes to OTHER FILES too**, which its name does not suggest — a declared-`readonly` checkout refuses `VACUUM INTO` with `SQLITE_READONLY`, so an honest declaration already closes that half and C3-7b must not serve the admin surface on such a checkout. **A capability boundary was surfaced and RAISED rather than patched, which is the slice's best lesson.** §12/D8 keeps the database path in the engine so PHP never learns it, yet `VACUUM INTO` lets the CLIENT name a path the engine writes a full copy to. Refusing the verb was the obvious fix — **and it would have been security theatre, which only a measurement showed**: `ATTACH DATABASE '<path>'` plus an ordinary `CREATE TABLE side.copy AS SELECT …` produces the same copy at the same arbitrary path, and `ATTACH` is permitted BY DESIGN (§22.2 (be)'s hygiene list exists to `DETACH` it). **With the negative control that makes it precise:** outside a transaction the attachment does NOT survive to the next statement, because `ATTACH` taints unconditionally ((bm)) — so the TRANSACTION carries the capability, not the verb. It is now a **§21 open item** with three options and an interim default of unchanged, and a **TRIPWIRE test asserts the capability** so a decision to close it turns that test RED rather than passing silently (the (bl) `foreign_keys` precedent, applied on purpose). **No engine code changed in C3-7a** — the output is a decision, a split, and seven tests pinning what the next slice builds on. **The owner then CHOSE confinement, and it is built as SPEC D14 (2026-09-15, §22.2 (bp); scope doc §22): the engine confines every file it opens on a client's behalf to one directory per pool.** **The enforcement point is SQLite's OWN AUTHORIZER, and finding that is what turned an expensive decision into a contained one** — `VACUUM INTO` and `ATTACH` both fire `SQLITE_ATTACH` carrying the RESOLVED filename before SQLite opens anything, so ONE guard covers both and any future verb that attaches a file, with no SQL parsing. Charter rule 6 never arises: nothing is rewritten and nothing is inferred from statement text; SQLite states which file it is about to open and the engine answers. **Measured on the BUNDLED 3.53.2 rather than the system 3.45.1** — C3-7a's probe ran against the latter and flagged the difference as a thing to CHECK, the eighth premise this project has refused to take on trust. A `Deny` is `SQLITE_AUTH` (23) and creates NO file, cleaner than the declared-readonly refusal which leaves a zero-byte one; `rusqlite`'s `hooks` feature adds no dependency. **The default is what makes the guard safe without configuration:** `allow_dir` is the database file's own directory unless `FERRO_POOL_<NAME>_ALLOW_DIR` widens it, so `VACUUM INTO 'snap.db'` beside the database works out of the box and no existing deployment changes. The root is canonicalised ONCE at dial (an unresolvable root is a connect failure, not a guard that silently allows nothing); a candidate's PARENT is canonicalised, since a snapshot's target does not exist yet; an EMPTY filename is allowed (a temporary/in-memory attachment writes no file); a BLANK env value reads as unset rather than as the empty path. **C3-7a's tripwire is INVERTED — it asserted the capability, green on purpose, so the open decision could not be forgotten, and it asserts the refusal now.** Three mutations, each failing exactly the tests that claim it: no guard → the 5 guard tests and not the 7 mechanism ones; guard on the fresh dial but not the reopen → ONLY `the_guard_survives_a_recycle` (which matters because (bm) made `Full` a close-and-reopen, so one-exit enforcement would depend on POOL OCCUPANCY — the C3-4 shape); path STRINGS instead of canonicalisation → ONLY the `..`-traversal test. **Cost stated:** an application that ATTACHes outside the allowed directory needs the operator to widen it; plain `VACUUM` — which stock Laravel's `dropAllTables()` ends in — is untouched, because it opens no file. **C6 is DONE (2026-09-15): the incompatibilities page gets its second tier, and a CI gate that would have caught the rot it was already carrying** (§22.2 (bq); `docs/known-incompatibilities.md`, `ci/check-incompatibilities-doc.sh`). §14 budgeted "the full per-package catalogue" for M2 and the page had only ever held ONE package — every entry written from the Doctrine tier at M1-S8b, with `ferro/laravel` entirely absent after three phases of measured behaviour. **The gate was built FIRST and it named the work**, which is also how the sharpest finding surfaced: the page was telling readers a `bigint` at or above 2^32 **could not be read** — a HIGH-severity defect FIXED at M1-S9 with a live test proving the whole int64 range — and that a dead backend could wedge a first query for the ~127 s OS connect timeout, bounded at Phase B. **A stale "this is broken" is worse than a stale README: it is believed, and worked around.** Prose cannot be checked; CITATIONS can, and this page's convention is that every entry carries one. `ci/check-incompatibilities-doc.sh` (a file check with no toolchain or backend, beside `ci/check-d12-recorded.sh` in the `rust` lane) fails the build when a link or `docs/…` path stops resolving, when a cited `§22.2 (xx)` letter is absent, when a cited `Ferro\…` name or `SomeTest::testFoo` no longer exists, when a follow-up declares no status, or — **the rule that catches the rot** — when the page cites a follow-up that has since been RESOLVED. A fixed follow-up is not deleted (its measurement is worth keeping); it stops being CITABLE as a live defect and the page points at the §22.2 entry that closed it. **Running it found that ALL NINE follow-ups are resolved** — three said so in three different vocabularies, four said nothing at all — and that the page cited two of them as live. Every status was re-derived against the TREE rather than copied from the document asserting it, which is how a second stale claim surfaced, this one in this very file: **the MySQL `CALL` blind spot was closed at (av)/(aw)** and only the `/proto` multi-result-set change remains deferred; corrected in both places it appeared. **Six checks, TEN mutations, and the round paid for itself three times:** the spec-letter check read only the FIRST letter of a list like `§22.2 (am), (ar)`, so a rotted second letter passed; the PHP-name and test checks grepped the **gitignored `php/*/vendor`** (absent from the CI lane) and matched on the LEAF name alone, so a cited `Ferro\\Nope\\Connection` passed against one of `doctrine/dbal`'s own dozen `Connection` classes — both now resolve against this repo's own sources with the NAMESPACE matched too; and tightening that exposed a citation that could never have resolved in CI (`TransactionTest::testCommitFailure` is UPSTREAM's, now written fully qualified). A fourth was silent: `decl_re` built its regex with `printf '…%s\\b'`, and **printf expands `\\b` in a FORMAT to a backspace**, so the pattern matched nothing. The Laravel half is COLLECTED from the recorded C2/C3-6b columns, not re-derived: the driver-NAME artifact with its two-column proof, the SQLite alias asymmetry (42 extra errors), `DB::escape()`/`toRawSql()` refused on a pool that does not advertise `literals_are_standard` (SQLite and MySQL today — `null` is fail-closed), the sticky `lastInsertId()`, the raw-string hand-off with its one deliberate RFC3339 divergence, `search_path` needing to be in two places, `FerroSQLiteBuilder`, the `compileAlter()` foreign-key refusal with its three measured dead ends — and the plain statement that **MySQL is not registered by this tier, so §15's bar is NOT met**. D14 gained a *Files and paths* section beside it. Entries whose defect is fixed are **rewritten in place and marked FIXED**, never quietly deleted: an entry public long enough to be believed needs a correction, not a silence. **C4a is DONE (2026-09-16): the §13 slow log — and the observability bullet order is decided by TRANSPORT, not by §13's own list** (§22.2 (br); §13 amended; `ferro-classify`'s `fingerprint.rs`, `ferrod`'s `slow_log.rs`). §13 names four things — OTLP traces, Prometheus, the slow log, `ferro top`. **Prometheus needs a scrape endpoint and `ferro top` needs the admin service, and `ADMIN = 5` still has no method table ((bo))**, so both wait on C3-7b; a log line needs only the `tracing` subscriber the daemon has had since M0, which is exactly product-vision §5's *"structured JSON to stdout/journald"*. So the slow log shipped first, **and it shipped the piece the other three all reuse: the fingerprint.** **The redaction contract is STRUCTURAL rather than procedural:** `ferro_classify::Fingerprint` is a newtype whose ONLY constructor is the normalizer (grep-verified — one construction site in the workspace), so *"log the raw SQL"* **cannot be written at the call site at all**, which is stronger than a test catching it and is why the type exists instead of a `String` plus a review rule. Parameters are not a field of the record — `params` is a COUNT — and **`log_params = always` is deliberately NARROWED to each parameter's TYPE and LENGTH**, with §13 amended to say so: the operator who flips it to debug a slow query is not consenting on behalf of the data subject, and a type and a length identify which bind went wrong without the log becoming a place secrets live. The same reasoning gives the error field the `PoolError` VARIANT and never its message, because a backend's error text quotes the offending value (`Key (email)=(alice@example.com) already exists`). **It reuses the pin lexer's region pass rather than writing a second one, and that is a redaction decision:** that scanner already knows what a literal is on each dialect (E-strings, `''` escapes, dollar-quoting, nested block comments) and is total on any input, so a second "good enough" literal finder would be a second place for the contract to be wrong. It gained exactly one thing — each hidden span's KIND — which is **underivable from the masked copy**, since a space INSIDE a literal is byte-identical to a space in code. **Three things measured rather than assumed:** `exec_us`/`queue_us` were ALREADY computed and already on the wire, so the record observes the §13 split rather than adding timing; the threshold is on **queue + exec**, because a slow log counting only `exec_us` is silent on pool exhaustion, the commonest production stall; and a write reports rows AFFECTED, since `rows.len()` is 0 for every INSERT. **THE MUTATION ROUND FOUND TWO WEAK TESTS OF THIS SLICE'S OWN** — deleting the call site left all six unit tests green and failed only the e2e (the C3-4 shape, and the reason the e2e exists), but *"render params regardless of `log_params`"* ALSO passed at first, because every statement in the e2e carried its values as LITERALS and so had no bound parameters: **the whole `log_params` matrix was unreachable end to end.** A parameterized statement was added and it fails now. **The generalisable lesson: a redaction knob can only be tested by a statement that actually carries the thing being redacted.** The e2e runs on SQLite (no server, runs in CI) at threshold `Some(0)` so it asserts WIRING and never timing — and writing it surfaced D14/C3-4 working as designed, refusing its own `CREATE TABLE` with `SQLITE_READONLY` until it declared itself a write. **Found and recorded at C4a, FIXED at C4b-1:** §13's pin-cause label vocabulary was declared CLOSED and was missing `SessionTracker`, added to `PinCause` at M1-S6 — eight labels against nine variants. (C4a filed it as “FB-7”, an ID C1b's ledger already used; the duplicate label is retired with the fix.) **C4b-1 is DONE (2026-09-16): the §13 Prometheus endpoint — and the slice was named by a WRONG CONCLUSION IN MY OWN PREVIOUS ENTRY** (§22.2 (bs); `ferrod`'s `metrics.rs`, `ferro-pool`'s `pin.rs`). §22.2 (br) split C4 by transport and got the principle right, then concluded that Prometheus and `ferro top` **both** wait on C3-7b's admin service. **They do not.** §13 calls `ferro top` a *"live TUI over the admin service"* in so many words and gives the Prometheus bullet **no transport at all**, because a scrape is an HTTP GET needing no `/proto` surface; and product-vision §5's *"admin/metrics bind loopback/UDS by default"* is a stated default, so the endpoint never needed the cross-host authorization model that IS owner-only for C3-7b. One iteration later that conclusion came back as a premise in the loop's own trigger prompt. **The guard is the one already applied to the engine: check the claim against the tree, including when the claim is your own.** **No HTTP dependency was added** — the workspace had no hyper/axum/http/metrics crate, and a scrape is one GET on one path, so the responder is hand-rolled (bounded 8 KiB head, 5 s read deadline, no body read, three response shapes) rather than pulling a dependency tree into a daemon holding every tenant's credentials, which is what D9's pool and the M0 codec already do. **The exposition text is built separately from the socket**, so the format dashboards depend on is a pure function under test while the listener is tested for what listeners get wrong; connections are served ONE at a time, because loopback is not nobody. **Binding is OPT-IN, a deliberate reading of §5 rather than a contradiction of it:** "loopback by default" says WHERE the endpoint binds once asked for, not WHETHER a daemon that has only ever had one listener starts opening a port because a package was upgraded — so `FERRO_METRICS_ADDR` defaults unset, a blank value reads as unset (D14's `allow_dir` rule), **a bind failure logs at `error` and never takes the daemon down** (an observability fault must not become an outage), and a non-loopback bind is warned about by name since the endpoint is unauthenticated. **The pin-cause vocabulary bug is CLOSED so it cannot recur:** §13 declared the vocabulary closed while `PinCause` had gained `SessionTracker` at M1-S6, and it rotted precisely because the vocabulary was a hand-kept list — now `index()` is an exhaustive match so **a new variant does not compile** without a slot, a coverage test catches a bad slot or a variant missing from `ALL`, and **a test parses §13's own sentence out of the spec file** and compares it to the enum, so the two cannot drift in either direction. One level down, `last_pin_cause` became a `PinCauseCell` whose inner field is private to `pin.rs`, so **a pin site that sets a cause without counting it fails to compile — verified by writing one, not asserted** (the C4a `Fingerprint` precedent). **Every cause is exported for every pool including the zeroes**, because "no data" and "it has not happened" must not be the same observation to an alert; the `cause` label is a closed vocabulary of bare identifiers but the `pool` label is operator-supplied, so it is escaped — the one value that could close its quotes and forge a series. **Two of this slice's own defects were caught by its own tests:** the escape assertion was wrong (a correctly-escaped `\",` still contains `",`; it now asserts every quote is backslash-preceded), and the e2e helper dropped the watch sender so `serve` returned before the first request, surfacing as a `ConnectionRefused` that looked like a bind failure. **Deferred and named:** §13's remaining metrics are **C4b-2**; **C4c (OTLP)** needs a `traceparent` on the EXEC options and **nothing in the tree produces one today**, so its consumer wants checking first; **C4d (`ferro top`)** genuinely does wait on C3-7b.

## What Ferro is

A **per-host database access engine for PHP**. One Rust daemon (`ferrod`, tokio) owns all upstream DB connections, pools them in transaction mode, and multiplexes many PHP-FPM workers over a local Unix socket — while remaining a **drop-in** replacement for Doctrine DBAL and Laravel Eloquent (config-only adoption).

It is deliberately **not**: a database, a SQL rewriter, a result cache, or an ORM-in-Rust. The engine's job ends where live PHP object semantics begin. See Ground rule 6 and SPEC §3.

## Architecture at a glance

The whole design lives in the spec; this is the map, with section pointers so you read the 2–3 relevant sections, not all 47 KB.

```
PHP-FPM workers (N)  ── one UDS conn each, all requests multiplexed ──┐
  ferro/client         native typed API, Fibers-aware   §10, §10.1   │
  ferro/doctrine-dbal  DBAL Driver SPI → client          §14         │
  ferro/laravel        Illuminate Connection → client    §15         │
        │  UDS /run/ferro/{schema_hash}.sock  (TCP fallback FERRO_ADDR)
        ▼
  ferrod (Rust/tokio)                                                │
   session layer   handshake+epoch, auth, multiplexing, PING/PONG  §5
   SQL service     EXEC: param bind, result framing                §6
   TX service      tx_id pinning, savepoints, deadlines            §6, §7
   pin engine      protocol signals first, assist lexer second     §7.1–7.2
   pools           pg | mysql | sqlite | mssql (+ replicas)        §7, §8
   manifest store  query_id → plan (checked SQL / codegen)         §11
   admin/metrics   OTLP, Prometheus, slow log                      §13
```

Load-bearing ideas that shape almost every change (all enshrined in the charter's Ground rules):

- **Transactions pin to a `tx_id`, not to the client socket** (§4, §7). This is what makes Fiber-suspended requests and multiplexing correct.
- **Every in-flight request ends in exactly one `END` frame** (§5.2). All session state machines key off this.
- **The `Indeterminate` error branch** (§9.2, §19.3) — a write whose fate is unknown — is the spec's defining safety property. The engine *never* transparently retries; it classifies and reports, and retry is client policy. Auto-retry of an indeterminate write is licensed *only* by a manifest `idempotent: true`.
- **`boot_epoch`** (§5, §19.1): a changed epoch on reconnect voids all engine-side state; the client resilience loop is built around it.
- **`/proto` is the single source of truth** for method ids, flags, error codes, type tags (§20.2). Hand-written protocol constants in Rust or PHP are a defect.

## Where code will live (planned, SPEC §20.1)

```
/engine                     Cargo workspace (edition 2024, tokio multi-thread)
  crates/ferro-proto        wire types, codec, generated consts, vector tests
  crates/ferro-classify     assist lexer (§7.1)
  crates/ferro-pool         hand-rolled pool + pin state machine (D9)
  crates/ferro-backend-{pg,mysql,sqlite,mssql}
  crates/ferrod             daemon binary (session layer, services, admin)
  crates/ferro-cli          `ferro` CLI: schema sync / check / gen (D10)
/php
  client/                   ferro/client            (PSR-4 `Ferro\`, PHP ≥ 8.2)
  doctrine-dbal/            ferro/doctrine-dbal-driver
  laravel/                  ferro/laravel
/proto                      methods.toml, errors.toml, types.toml, vectors/
/bench                      harness + committed results with env manifests
/testkit                    docker-compose (pg, mysql, mssql), fixtures, suite runners
```

## Spec reading map (by task)

- **Any protocol/wire work** → §5, §5.1 (memfd), §5.2 (session/stream rules), §20.2. Update `/proto` + golden vectors + both codecs in the same change.
- **Pooling / pinning / hygiene** → §7.1–7.7; pin-cause labels in §13; the transaction-mode contract limitation in §7.4.
- **Errors / resilience / restart** → §9.2 (taxonomy), §19 (epochs, reconnect loop, fate matrix). Chaos test is the acceptance bar (§20.3).
- **Type system / hydration** → §9, §9.1 (policies-over-guesses).
- **Doctrine tier** → §14. **Eloquent tier** → §15. Both keep stock Grammar/Processor/platforms; only the execution layer changes.
- **Checked SQL / codegen / manifest** → §11.
- **Bench / M0 exit gate** → §16, §16.1 (the D12 p99 decision gate).
- **Deployment / lifecycle** → §18 (systemd socket activation), §7.7.
- **Decisions already settled** → §21 (D1–D12). Binding; don't re-litigate.

---

# Charter (the working agreement)

## Ground rules

1. **Decisions in SPEC §21 are binding.** Do not re-litigate them in code, comments, or refactors. If one proves impossible, stop and raise it — don't route around it.
2. **`/proto` is the single source of truth** for method ids, flags, error codes, and type tags. Any protocol change updates the registry, the golden vectors, and both codecs (Rust + PHP) in the same change set. Hand-written protocol constants anywhere are a defect.
3. **The engine never transparently retries user statements** (SPEC §3, §19.3). It classifies and reports; retry is client policy. This is load-bearing for the `Indeterminate` guarantee — do not "helpfully" add engine retries.
4. **Every in-flight request terminates in exactly one END frame.** All session-layer code is written against that invariant.
5. **Correctness over throughput until the M0 gate.** Optimize only against recorded bench numbers (SPEC §16.1), never speculatively.
6. **Scope discipline:** no ORM semantics in Rust, no SQL rewriting, no result caching, no read/write inference (SPEC §3). The drop-in tiers change execution, never SQL generation — Grammar/Processor and DBAL platforms stay stock.
7. **PHP client stays dependency-free at runtime.** Optional extensions (`ext-msgpack`, `ext-sockets`) are runtime-detected, never required.

## Build order

Follow SPEC §17 milestones; within M0, execute §17.1 tasks in order. M0 does not exit until the D12 bench measurement is recorded in `bench/results/` with its environment manifest.

## Definition of done (every task)

- Tests: unit + (where applicable) integration against `/testkit` backends; protocol work adds/updates golden vectors; pin-engine work adds a pin-cause assertion; §19 work extends the chaos harness.
- Gates green: `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`, PHPUnit, PHPStan level 9 on `php/client`.
- The relevant SPEC section still tells the truth. If implementation forced a deviation, amend the spec text and add a line to §22 in the same change.

## Commands

```
docker compose -f testkit/docker-compose.yml up -d     # backends
cargo test --workspace                                  # engine
(cd php/client && composer install && composer test)    # client
cargo run -p ferro-bench -- --baseline pdo              # M0 harness
```

Single-test invocations (once the workspaces exist):

```
cargo test -p ferro-proto codec::vectors                # one Rust test/module
cargo test --workspace -- --nocapture <name>            # by name, with output
(cd php/client && ./vendor/bin/phpunit --filter testFoo) # one PHPUnit test
(cd php/client && ./vendor/bin/phpstan analyse --level 9 src)  # static analysis
```

## When uncertain

- Protocol or semantic ambiguity → add it to SPEC §21 open items and ask; do not guess on the wire format or the fate rules.
- Everything else → make the smallest reasonable call, note it in SPEC §22, keep moving.
